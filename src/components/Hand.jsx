import React from 'react';

const Hand = props => {return (
    <div className = "Hand" id = "HandSpace">
      <button className="DrawButton">Draw a Card</button>
    </div>
  )
}

export default Hand;